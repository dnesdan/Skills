interface:
  display_name: "Enable Computer Use"
  short_description: "Repair Dan's Codex Computer Use plugin setup."
  default_prompt: "Use $computer-use-enable to inspect my Codex config, enable the bundled Computer Use plugin if needed, and verify what still requires a Codex restart."
